sudo: required
language: generic 
services:
  - docker

before_install:
  - docker build -t sherrajput/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true sherrajput/docker-react npm run test